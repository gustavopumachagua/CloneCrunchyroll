import { FooterSection } from "../FooterSection/index";
import { accountItems } from "../footerItems";

const AccountSection = () => {
  return <FooterSection title="Cuenta" items={accountItems} />;
};

export default AccountSection;
